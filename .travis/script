#!/usr/bin/env bash

set -x

tail -F $HOME/.kerl/builds/master/otp_build_git.log &
TAIL_PID=$!

. ./kerl build git https://github.com/erlang/otp.git master master
. ./kerl install master master

kill $TAIL_PID
